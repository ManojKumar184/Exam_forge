/**
 * @deprecated Use `useAuth()` from `src/hooks/useAuth` (AuthContext + MERN API).
 * This Zustand store is retained temporarily for reference during migration.
 */
export { useAuth as useAuthStore } from '../hooks/useAuth';
